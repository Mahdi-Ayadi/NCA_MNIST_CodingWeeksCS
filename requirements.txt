To run the application, you need to install the required libraries. You can do this by running the following command in the terminal:

Numpy installation: pip install numpy
Torch installation: pip install torch
Pygame installation: pip install pygame
matplotlib installation: pip install matplotlib
Tkinter installation: pip install tk